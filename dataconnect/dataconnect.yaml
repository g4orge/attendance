specVersion: "v1beta"
serviceId: "attendance"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "attendance-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
